# ProWine 網站維護指南

**網站地址**: https://prowine.vercel.app/  
**GitHub 倉庫**: https://github.com/justsum66/prowine.git  
**部署平台**: Vercel

---

## ⚠️ 重要：不要刪除本地文件夾！

**本地文件夾是必需的，原因：**

1. **持續開發**：所有新功能、修復、更新都需要在本地開發
2. **版本控制**：Git 倉庫在本地，刪除後無法追蹤變更
3. **測試環境**：本地可以安全測試，不會影響生產環境
4. **備份**：本地代碼是重要的備份
5. **調試**：生產環境問題需要在本地重現和修復

**建議**：保留本地文件夾，定期備份到外部硬碟或雲端

---

## 📋 日常維護工作流程

### 1. 更新網站內容（最常見）

#### 更新酒款資料
```bash
# 1. 在本地編輯代碼或運行腳本
npm run scrape:wines          # 爬取新酒款
npm run import:wine-data       # 導入到資料庫

# 2. 測試本地
npm run dev
# 訪問 http://localhost:3000 檢查

# 3. 提交並推送
git add .
git commit -m "更新：新增酒款資料"
git push

# 4. Vercel 會自動部署（通常 2-3 分鐘）
```

#### 更新酒莊資料
```bash
npm run scrape:wineries        # 爬取酒莊資料
npm run scrape:winery-logos    # 更新酒莊 Logo

# 測試 → 提交 → 推送
```

#### 更新圖片
```bash
npm run scrape:images          # 爬取圖片
npm run scrape:images-for-import  # 為導入準備圖片

# 測試 → 提交 → 推送
```

### 2. 修復 Bug 或添加功能

```bash
# 1. 在本地開發
npm run dev

# 2. 測試
npm run lint                   # 檢查代碼質量
npx tsc --noEmit --skipLibCheck  # 檢查類型錯誤
npm run build                  # 測試構建

# 3. 提交並推送
git add .
git commit -m "修復：描述問題"
git push

# 4. Vercel 自動部署
```

### 3. 更新依賴套件

```bash
# 1. 檢查過期套件
npm outdated

# 2. 更新套件（謹慎！）
npm update

# 3. 測試
npm run dev
npm run build

# 4. 提交並推送
git add package.json package-lock.json
git commit -m "更新：依賴套件"
git push
```

---

## 🔍 監控與檢查

### 每日檢查（5 分鐘）

1. **訪問網站**
   - 打開 https://prowine.vercel.app/
   - 檢查首頁、酒款頁面、酒莊頁面是否正常
   - 測試搜尋功能

2. **檢查 Vercel Dashboard**
   - 登入 https://vercel.com/dashboard
   - 查看最新部署狀態
   - 檢查是否有構建失敗

3. **檢查 Sentry（如有配置）**
   - 登入 Sentry Dashboard
   - 查看是否有新的錯誤報告

### 每週檢查（30 分鐘）

1. **性能檢查**
   ```bash
   # 在本地運行性能測試
   npm run test:all
   ```

2. **資料庫檢查**
   - 登入 Supabase Dashboard
   - 檢查資料庫使用量
   - 檢查是否有異常查詢

3. **備份檢查**
   - 確認本地代碼已提交到 GitHub
   - 確認重要資料已備份

### 每月檢查（1 小時）

1. **安全更新**
   ```bash
   npm audit                    # 檢查安全漏洞
   npm audit fix               # 自動修復（謹慎使用）
   ```

2. **依賴更新**
   ```bash
   npm outdated                 # 檢查過期套件
   ```

3. **性能優化**
   - 檢查 Lighthouse 分數
   - 優化圖片大小
   - 檢查 API 響應時間

---

## 🚨 緊急情況處理

### 網站無法訪問

1. **檢查 Vercel Dashboard**
   - 登入 https://vercel.com/dashboard
   - 查看部署狀態
   - 檢查構建日誌

2. **檢查 GitHub**
   - 確認最新代碼已推送
   - 檢查是否有構建錯誤

3. **回滾到上一個版本**
   - 在 Vercel Dashboard 中選擇之前的部署
   - 點擊 "Promote to Production"

### 資料庫連接失敗

1. **檢查 Supabase**
   - 登入 Supabase Dashboard
   - 檢查服務狀態
   - 檢查環境變數

2. **檢查環境變數**
   - 在 Vercel Dashboard 中檢查環境變數
   - 確認 `NEXT_PUBLIC_SUPABASE_URL` 和 `NEXT_PUBLIC_SUPABASE_ANON_KEY` 正確

### 構建失敗

1. **查看構建日誌**
   - 在 Vercel Dashboard 中查看詳細錯誤
   - 複製錯誤訊息

2. **本地重現問題**
   ```bash
   # 清除緩存
   rm -rf .next node_modules
   
   # 重新安裝
   npm install
   
   # 本地構建
   npm run build
   ```

3. **修復並推送**
   ```bash
   git add .
   git commit -m "修復：構建錯誤"
   git push
   ```

---

## 📝 常見維護任務

### 添加新酒款

1. 在 Supabase Dashboard 中手動添加，或
2. 運行爬蟲腳本：
   ```bash
   npm run scrape:wines
   npm run import:wine-data
   ```

### 更新價格

```bash
npm run scrape:prices
```

### 檢查缺失資源

```bash
npm run check:missing-assets
```

### 檢查文案

```bash
npm run check:copywriting
```

---

## 🔐 安全維護

### 定期更新環境變數

1. **檢查 API 金鑰是否過期**
   - Supabase 金鑰
   - Google AI API 金鑰
   - Groq API 金鑰
   - Resend API 金鑰

2. **更新 Vercel 環境變數**
   - 登入 Vercel Dashboard
   - 進入 Project Settings → Environment Variables
   - 更新過期的金鑰

### 檢查安全漏洞

```bash
npm audit
npm audit fix
```

---

## 📊 性能監控

### Vercel Analytics（可選）

1. 在 Vercel Dashboard 中啟用 Analytics
2. 查看訪問統計、性能指標

### Sentry 錯誤追蹤（已配置）

1. 登入 Sentry Dashboard
2. 查看錯誤報告
3. 修復高優先級錯誤

### 本地性能測試

```bash
npm run test:smoke    # 冒煙測試
npm run test:api     # API 測試
npm run test:stress  # 壓力測試
```

---

## 🛠️ 開發環境設置

### 首次設置（新電腦）

```bash
# 1. 克隆倉庫
git clone https://github.com/justsum66/prowine.git
cd prowine

# 2. 安裝依賴
npm install

# 3. 配置環境變數
cp env.template .env.local
# 編輯 .env.local，填入實際值

# 4. 運行開發服務器
npm run dev
```

### 更新本地代碼

```bash
# 從 GitHub 拉取最新代碼
git pull origin main

# 更新依賴
npm install

# 運行開發服務器
npm run dev
```

---

## 📞 獲取幫助

### 文檔資源

- [Vercel 部署指南](./deployment/VERCEL_SETUP_GUIDE.md)
- [GitHub Actions CI 修復](./deployment/GITHUB_ACTIONS_CI_FIX.md)
- [Web Push 修復](./deployment/WEB_PUSH_FIX_COMPLETE.md)

### 檢查清單

- [部署檢查清單](../DEPLOYMENT_CHECKLIST.md)
- [README 部署指南](../README_DEPLOYMENT.md)

### 聯繫方式

- **GitHub Issues**: https://github.com/justsum66/prowine/issues
- **Vercel Support**: https://vercel.com/support

---

## ✅ 維護檢查清單

### 每日
- [ ] 訪問網站確認正常運行
- [ ] 檢查 Vercel Dashboard 部署狀態

### 每週
- [ ] 檢查 Sentry 錯誤報告
- [ ] 檢查 Supabase 資料庫狀態
- [ ] 運行本地測試

### 每月
- [ ] 更新依賴套件
- [ ] 檢查安全漏洞
- [ ] 性能優化檢查
- [ ] 備份重要資料

---

## 🎯 最佳實踐

1. **小步提交**：每次修改後立即提交，不要累積大量變更
2. **測試先行**：在本地測試後再推送
3. **查看日誌**：遇到問題先查看 Vercel 構建日誌
4. **定期備份**：定期將本地代碼備份到外部硬碟或雲端
5. **文檔更新**：重要變更後更新文檔

---

**最後更新**: 2025-12-02  
**維護者**: ProWine 開發團隊

